/*import {test, expect} from '@playwright/test'
import * as SignUp from '../POM/Signup'
import { CreateInbox } from '../utils/EmailUtils'

test ('Sign Up', async ({page}) => {

const inbox = await CreateInbox();
console.log(inbox)    
await page.goto('https://valentinos-magic-beans.click/')
await page.getByRole('button', { name: 'Sign Up' }).click()

SignUp.SignUpEmptyForm(page)
SignUp.SignUpwithIncorrectPassword(page)
SignUp.SignUpWithValidData(page)


})
*/

 